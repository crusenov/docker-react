language: generic 

sudo: required
services:
  - docker 

before_install:
  - docker build -t crusenov/docker-react -f Dockerfile.dev .

  script:
    - docker run -e CI=true crusenov/docker-react npm run test